query Estaciones($pagination: PaginationArg) {
  estaciones(pagination: $pagination) {
    data {
      id
      attributes {
        nombre
        codigoNFC
        createdAt
        updatedAt
      }
    }
    meta {
      pagination {
        total
      }
    }
  }
}
