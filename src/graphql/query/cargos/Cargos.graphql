query Cargos($pagination: PaginationArg) {
  cargos(pagination: $pagination) {
    data {
      id
      attributes {
        nombre
        estado
        createdAt
        updatedAt
      }
      id
    }
    meta {
      pagination {
        total
      }
    }
  }
}
